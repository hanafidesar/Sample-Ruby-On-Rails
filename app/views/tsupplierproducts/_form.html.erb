<%= form_with(model: tsupplierproduct, local: true) do |form| %>
  <% if tsupplierproduct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tsupplierproduct.errors.count, "error") %> prohibited this tsupplierproduct from being saved:</h2>

      <ul>
      <% tsupplierproduct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.collection_select :product_id, Product.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :supplier_id %>
    <%= form.collection_select :supplier_id, Supplier.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
