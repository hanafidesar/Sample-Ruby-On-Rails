<%= form_with(model: table_customer_product, local: true) do |form| %>
  <% if table_customer_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(table_customer_product.errors.count, "error") %> prohibited this table_customer_product from being saved:</h2>

      <ul>
      <% table_customer_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.collection_select :product_id, Product.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.collection_select :customer_id, Customer.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
