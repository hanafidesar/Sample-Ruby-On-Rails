<%= form_with(model: tcustomerproduct, local: true) do |form| %>
  <% if tcustomerproduct.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tcustomerproduct.errors.count, "error") %> prohibited this tcustomerproduct from being saved:</h2>

      <ul>
      <% tcustomerproduct.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.collection_select :customer_id,Customer.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.collection_select :product_id,Product.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
