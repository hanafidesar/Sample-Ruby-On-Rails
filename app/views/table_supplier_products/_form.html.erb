<%= form_with(model: table_supplier_product, local: true) do |form| %>
  <% if table_supplier_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(table_supplier_product.errors.count, "error") %> prohibited this table_supplier_product from being saved:</h2>

      <ul>
      <% table_supplier_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :supplier_id %>
    <%= form.collection_select :supplier_id, Supplier.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.collection_select :product_id,  Product.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
